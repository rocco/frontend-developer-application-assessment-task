import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend-developer-application-assessment-task',
  webDir: 'www'
};

export default config;
